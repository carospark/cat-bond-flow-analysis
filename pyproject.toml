[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cat-bond-flow-analysis"
version = "0.1.0"
description = "Joining catastrophe bond issuance to secondary-market and fund data"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["pandas", "numpy", "requests", "beautifulsoup4", "lxml"]

[tool.setuptools]
py-modules = []
